---
const { greeting, name, subtitle } = Astro.props;
---

<h1>
    <span class="greeting">{greeting}</span><br />
    <span class="name">I’m {name}</span>
</h1>

<p class="subtitle">{subtitle}</p>
